export class CartItemFactory {
  /**
   * Check if adding first time
   * Check if items exist - increment, decrement
   * Check if removing item completely
   * Calculate CartPrice
   * Update Cart Items
   */
}
